@model IEnumerable<RecruitmentApplication.Models.TraitCategory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<button onclick="location.href = '/TraitCategories/Create';" class="mdl-cell mdl-cell--1-col mdl-button mdl-js-button mdl-button--raised mdl-js-ripple-effect">
    <i class="material-icons">add</i>Create
</button>
<table class="mdl-data-table mdl-js-data-table mdl-shadow--2dp">
    <thead>
        <tr>
            <th class="mdl-data-table__cell--non-numeric">Trait Name</th>
            <th>Trait Description</th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td data-label="Name" class="mdl-data-table__cell--non-numeric left">
                @Html.DisplayFor(modelItem => item.TraitName)
            </td>
            <td data-label="Surname" class="left">
                @Html.DisplayFor(modelItem => item.TraitDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.TraitID }) |
                @Html.ActionLink("Details", "Details", new { id = item.TraitID}) |
                @Html.ActionLink("Delete", "Delete", new { id = item.TraitID })
            </td>
        </tr>
    }

</table>